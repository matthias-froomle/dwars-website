# DWARS 2026

Standalone Drupal 11 theme generated against the Drupal 11.3.11 Starterkit
contract and implemented from the UI prototype in the repository root.

Build with `npm install && npm run build`; validate with `npm run check`. See
`docs/drupal-theme.md` in the parent repository for local restore, edition import,
deployment, and rollback instructions.
