{% if items.previous or items.next %}
  <nav class="pager pager--mini" role="navigation" aria-labelledby="{{ heading_id }}">
    <{{ pagination_heading_level }} id="{{ heading_id }}" class="visually-hidden">{{ 'Pagination'|t }}</{{ pagination_heading_level }}>
    <ul class="pager__items js-pager__items">
      {% if items.previous %}
        <li class="pager__item pager__item--previous">
          <a href="{{ items.previous.href }}" title="{{ 'Go to previous page'|t }}" rel="prev"{{ items.previous.attributes|without('href', 'title', 'rel') }}>
            <span class="visually-hidden">{{ 'Previous page'|t }}</span>
            <span aria-hidden="true">{{ 'Vorige'|t }} ‹</span>
          </a>
        </li>
      {% endif %}
      {% if items.current %}
        <li class="pager__number-strip">
          <ol class="pager__pages">
            <li class="pager__item pager__item--page is-active">
              <span aria-current="page"><span class="visually-hidden">{{ 'Page'|t }}</span>{{ items.current }}</span>
            </li>
          </ol>
        </li>
      {% endif %}
      {% if items.next %}
        <li class="pager__item pager__item--next">
          <a href="{{ items.next.href }}" title="{{ 'Go to next page'|t }}" rel="next"{{ items.next.attributes|without('href', 'title', 'rel') }}>
            <span class="visually-hidden">{{ 'Next page'|t }}</span>
            <span aria-hidden="true">{{ 'Volgende'|t }} ›</span>
          </a>
        </li>
      {% endif %}
    </ul>
  </nav>
{% endif %}
